#include<stdio.h>
int main()
{
    int n,rem;
    int a[100];
    printf("enter the number:");
    scanf("%d",&n);
    int k=0;
    while(n>0)
    {
        rem=n%2;
        n=n/2;
        a[k]=rem;
        k++;
        
    }
    for(int y=k-1;y>=0;y--)
    {
        printf("%d",a[y]);
    }
}
